services:
  - type: web
    name: seqtrainer-benchlab
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DELETE_DATASETS_AFTER_RUN
        value: "true"
